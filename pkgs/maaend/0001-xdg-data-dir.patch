diff --git a/agent/cpp-algo/source/main.cpp b/agent/cpp-algo/source/main.cpp
index 3a0e19ac..3e7ddf43 100644
--- a/agent/cpp-algo/source/main.cpp
+++ b/agent/cpp-algo/source/main.cpp
@@ -1 +1,3 @@
+#include <cstdlib>
+#include <filesystem>
 #include <iostream>
@@ -16,2 +18,17 @@
 
+namespace {
+std::filesystem::path getToolkitUserPath()
+{
+    if (const char* xdg_data_home = std::getenv("XDG_DATA_HOME"); xdg_data_home && *xdg_data_home) {
+        return std::filesystem::path(xdg_data_home) / "MXU" / "debug" / "cpp-algo";
+    }
+
+    if (const char* home = std::getenv("HOME"); home && *home) {
+        return std::filesystem::path(home) / ".local" / "share" / "MXU" / "debug" / "cpp-algo";
+    }
+
+    return std::filesystem::path(".") / "debug" / "cpp-algo";
+}
+}
+
 int main(int argc, char** argv)
@@ -37,3 +54,5 @@ int main(int argc, char** argv)
 
-    MaaToolkitConfigInitOption("./debug/cpp-algo", "{}");
+    const std::filesystem::path toolkit_user_path = getToolkitUserPath();
+    const std::string toolkit_user_path_str = toolkit_user_path.string();
+    MaaToolkitConfigInitOption(toolkit_user_path_str.c_str(), "{}");
 
diff --git a/agent/go-service/creditshopping/storage.go b/agent/go-service/creditshopping/storage.go
index 412bbfa5..efbe65c3 100644
--- a/agent/go-service/creditshopping/storage.go
+++ b/agent/go-service/creditshopping/storage.go
@@ -20,3 +20,13 @@ var resolveShelfSnapshotPathFunc = defaultShelfSnapshotPath
 func defaultShelfSnapshotPath() string {
-	return filepath.Join("debug", "record", shelfSnapshotFileName)
+	return filepath.Join(mxuDataDir(), "debug", "record", shelfSnapshotFileName)
+}
+
+func mxuDataDir() string {
+	if xdg := os.Getenv("XDG_DATA_HOME"); xdg != "" {
+		return filepath.Join(xdg, "MXU")
+	}
+	if home, err := os.UserHomeDir(); err == nil && home != "" {
+		return filepath.Join(home, ".local", "share", "MXU")
+	}
+	return "."
 }
diff --git a/agent/go-service/logger.go b/agent/go-service/logger.go
index e32047dd..a2dd1110 100644
--- a/agent/go-service/logger.go
+++ b/agent/go-service/logger.go
@@ -29,4 +29,22 @@ func (w *levelFilterWriter) WriteLevel(level zerolog.Level, p []byte) (n int, er
 
+func getMxuDataDir() string {
+	xdgDataHome := os.Getenv("XDG_DATA_HOME")
+	if xdgDataHome != "" {
+		return filepath.Join(xdgDataHome, "MXU")
+	}
+
+	homeDir, err := os.UserHomeDir()
+	if err != nil || homeDir == "" {
+		return "."
+	}
+
+	return filepath.Join(homeDir, ".local", "share", "MXU")
+}
+
+func getDebugDir() string {
+	return filepath.Join(getMxuDataDir(), "debug")
+}
+
 func initLogger() (*os.File, error) {
-	debugDir := filepath.Join(".", "debug")
+	debugDir := getDebugDir()
 	if err := os.MkdirAll(debugDir, 0755); err != nil {
diff --git a/agent/go-service/main.go b/agent/go-service/main.go
index dadc9f06..dec18698 100644
--- a/agent/go-service/main.go
+++ b/agent/go-service/main.go
@@ -99,3 +99,3 @@ func main() {
 	// Initialize toolkit config option
-	userPath := getCwd()
+	userPath := getMxuDataDir()
 	if err := maa.ConfigInitOption(userPath, "{}"); err != nil {
