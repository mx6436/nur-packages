diff --git a/agent/cpp-algo/source/main.cpp b/agent/cpp-algo/source/main.cpp
index e25f9035..564b53d3 100644
--- a/agent/cpp-algo/source/main.cpp
+++ b/agent/cpp-algo/source/main.cpp
@@ -1,3 +1,4 @@
+#include <cstdlib>
 #include <filesystem>
 #include <iostream>
 #include <string>
@@ -26,13 +27,24 @@

 namespace
 {
-constexpr const char* kLogDir = "./debug/cpp-algo/debug";
+std::filesystem::path get_user_path()
+{
+    if (const char* xdg_data_home = std::getenv("XDG_DATA_HOME"); xdg_data_home && *xdg_data_home) {
+        return std::filesystem::path(xdg_data_home) / "MXU" / "debug" / "cpp-algo";
+    }
+
+    if (const char* home = std::getenv("HOME"); home && *home) {
+        return std::filesystem::path(home) / ".local" / "share" / "MXU" / "debug" / "cpp-algo";
+    }
+
+    return std::filesystem::path(".") / "debug" / "cpp-algo";
+}

 bool initialize_logging()
 {
     bool logging = true;
     MaaLoggingLevel stdout_level = MaaLoggingLevel_Error;
-    const auto config_path = MAA_NS::path("./config/maa_option.json");
+    const auto config_path = get_user_path().parent_path().parent_path() / "config" / "maa_option.json";
     std::error_code ec;
     const bool config_exists = std::filesystem::exists(config_path, ec);
     if (ec) {
@@ -67,7 +79,7 @@ bool initialize_logging()
     }

     // AgentServer 只支持日志选项，图像保存等配置由主进程负责。
-    std::string log_dir = logging ? kLogDir : "";
+    std::string log_dir = logging ? (get_user_path() / "debug").string() : "";
     if (!MaaGlobalSetOption(MaaGlobalOption_LogDir, log_dir.data(), log_dir.size())) {
         std::cerr << "Failed to set AgentServer LogDir" << std::endl;
         return false;
@@ -99,7 +111,7 @@ int main(int argc, char** argv)
     }

     // 转储落到 maafw.log 同一目录，报 issue 打包日志时会一并带上。
-    common::InstallCrashHandler(MAA_NS::path(kLogDir));
+    common::InstallCrashHandler(get_user_path() / "debug");

     // 父进程一旦退出立刻结束自己，避免 MXU/MFAA 崩溃后 cpp-algo 残留。
     common::StartParentProcessWatcher();
